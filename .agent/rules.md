# Agent Workflow Rules

## Granular Micro-Commits Rule
- **Always commit frequently with maximum granular micro-commits.**
- Every function, UI component adjustment, schema update, asset addition, and bug fix must be staged and committed atomically using Conventional Commits syntax with realistic timestamp spacing.
- **Never lump multi-file features into single bulk commits.**
